# Retained public GeoSPARQL vocabulary profile

The preparation command and committed manifest select two original OGC files from repository commit `ac303373bd0cf149d31f110cf8c1ed281ff66c60`. Their combined retained canonical dataset contains 939 quads, 186,697 bytes, SHA-256 `7aeec4ce3f8837c56d7586ea8df0acd53755a92b11d51fb1e4165a5e6e9866ea`. Upstream bytes, ownership namespaces and declared metadata remain unchanged.

The native preparation journey passed in 25.92 seconds. It used actual API routes, restricted PostgreSQL and retained object storage in the disposable runtime, with synthetic principals and company scope. Publisher, release and profile each received a separate independent review; the maker's self-review was rejected with 403. Exact replay returned the already retained release. Twelve preparation boundary checks also passed. Evidence references are in `.finai/artifacts/nin62-geosparql-native.json` in `D:\FinAI\g8-ontology-import`.

The resulting reviewed vocabulary release is resource `77680129-4e46-5492-adea-81e2a48f0fbc`, version `1a113355-64cf-5332-bc6e-af9d1e885b96`, hash `b6148964aea2093ebfd71cd336e7adf45b7505dca449603a48302f3f59a5bffd`. Profile resource `967d527d-4366-5c2a-b9ee-03f4d8d2de5a`, version `60ec738a-e4dd-55b3-91a0-f221b756f88b`, hash `ec4a05add26c958eb471aa6d6685c2c4e311f661e3c894e618ca216f3f4c2faf` selects those two exact vocabulary graphs.

## Retained validation and report review

The separate G8-authored `g8-geosparql-vocabulary-shapes-1.ttl` checks nine selected vocabulary terms using fifteen substantive SHACL Core checks. It does not represent the OGC conformance suite. Its original byte hash is `3f2b02b097ac3e841899803009236e0558ba471a234a6f37bdde8df30ec9f9e9`.

The original vocabulary passed those checks. A separate in-memory copy with one `hasGeometry` range assertion removed produced exactly one violation; the retained original was untouched. The reviewed shape release and constraint profile then completed the actual retained validation run `ontology-validation:e64348f1-78b9-4f86-b436-cbe462fdb6b4`, including execution publication and an independent canonical report review. Report resource `93101b7d-294e-52fe-a10f-2e761bd1b5f1`, version `239d73e9-27fd-5d48-a0ee-1f4d647491f6`, hash `11af3257831530f879f4585f3630da15b3559eabf42a7603025fe49406cd6c54` pins the retained observation. The retained report document hash is `2abac6132705a149f914d5bf13d3de62a4b60a80939a1d29518676520c0c51e5`.

Exact request, plan, report, execution publication and independent review references are retained in `.finai/artifacts/nin62-geosparql-validation-native.json`. This journey exercised the shared persisted services directly; it is distinct from the earlier real Temporal and HTTP/browser proofs.

## Limits

These are authentic public vocabulary bytes in a synthetic local scope. They establish neither authentic company facts nor approved SOCAR alignment, geometry correctness, spatial function support, GeoSPARQL conformance, accounting authority, browser acceptance or release acceptance. The official upstream validator contains SPARQL constructs outside the approved Core-only validator policy and is not silently substituted or partially accepted. Canonical integration remains gated on the exact full API coverage result and the remaining runtime/browser verification.
