# NIN-62 engine/import/index foundation evidence

Candidate base: `3f6d5c0e59aa2f8b2b4b220419ae96395e9cd828`; isolated Windows checkout `D:\FinAI\g8-ontology-import`. Canonical mounted product was not changed during these checks. PostgreSQL and private object storage came from the disposable CI runtime, with unique synthetic entity scopes.

Executed focused evidence:

| Boundary | Result |
| --- | --- |
| RDF parser, canonicalization, import closure and worker caps | 48 tests passed |
| Native retained import, maker/checker publication, exact dependencies and refusal | 24 tests passed in 52.75 s |
| Derived Oxigraph index, corruption recovery, scope and resource budgets | 25 tests passed in 23.67 s |
| Native authenticated release/index/term inspection and historical/current authority | 19 tests passed in 40.52 s |
| Existing promotion, rollback and lifecycle regression | 3 tests passed in 14.34 s |
| Typed ontology SDK | 7 focused tests and strict TypeScript builds passed |
| Actual backend JSON through SDK | 3 operations accepted; 8 scope/hash tamper cases refused |
| API static checks | Ruff passed; mypy passed over 177 source files |
| Live local HTTP transport at candidate `e5b048d` | 3 CLI operations and 3 SDK operations passed against isolated API 8065; owned API stopped afterward |

Native wire capture SHA256: `e8dcc229330deec2c8145caabe74dbbe63c0fa7e40b9a1742fec5b889d5a4b2ed`.

Native cases include source withdrawal, future-effective editing heads with current-effective pins, same-proposal publisher changes, replay outside the shared publication lock, immutable release protection, cross-scope refusal, retained original-byte access before cache reads, historical preservation after publisher changes, and withdrawal between worker start and response. Index cases include physical database tampering, exact dataset-hash verification, generation reuse and bounded cache growth. Parser caps executed on Windows; Linux limits remain subject to CI execution.

The initial SDK wire verification replays captured real API JSON through an injected fetch transport. A subsequent isolated API process performed actual HTTP release inspection, index rebuild and bounded term inspection through both the CLI and SDK. Evidence is retained at `D:\FinAI\g8-ontology-import\.finai\artifacts\nin62-live-cli-sdk.json`; it records exact commit and resource pins. Both checks remain synthetic fixtures: neither establishes browser acceptance, authentic publisher licensing, standards conformance, SOCAR alignment, accounting correctness, scale benchmark or release acceptance.

The foundation and reviewed installer correction were integrated into canonical development at `0212bc4d8b7b23bd6f327c1b77e3a085d8b5d64e`. [Exact CI](https://github.com/keshelavanina93-prog/finailin1/actions/runs/34174363163) passed both required checks: 1,281 API tests passed, 18 skipped, 90.22% coverage, 600.75 seconds. The Linux production cap probe and full index cases passed. No coverage threshold was reduced.

The canonical reviewed installer published four missing contracts through ordinary independent review, preserving existing reviewed semantics. Mounted API/worker refresh and approved Function manifest refresh preserved all 37 movement-account values, the earlier retained descriptor, and 17 pre-existing local user files. Evidence: `D:\FinAI\finailinear1\.finai\artifacts\nin62-integrated-runtime.json` and `nin62-reviewed-catalog-install.json`. The web build was unchanged; this backend refresh did not repeat browser acceptance.

NIN-62 remains In Progress. Validation/profile implementation began afterward in isolation; semantic diff/upgrades, alignment, authentic consumers, NYX serialization and product rendering are not inferred from this foundation evidence.
