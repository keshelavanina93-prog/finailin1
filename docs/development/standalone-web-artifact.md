# Standalone web artifact

`scripts/package-web-artifact.py` collects a completed Next standalone build together with static assets and public files. Supply the materialized source directory, its verified source archive and a D-local output root. The expected entrypoint is `apps/web/server.js`. Tracing and Turbopack use the explicit monorepo source root, including when source is materialized inside another checkout.

Collection checks every tracked source file against the committed archive before and after packaging. Generated output does not substitute for that check. Dependencies must resolve within the recorded build roots; unsupported reparse points, escaping paths, directory cycles and duplicate destinations are refused. Standard Windows extended-length paths preserve the same containment requirement. The ZIP contains regular copied bytes with per-file hashes and a receipt identifying the original source commit and archive.

This collector verifies source inputs and collected output. It does not independently prove the earlier build invocation, dependency installation, absence of embedded environment values or runtime acceptance. Local credential files are excluded by name, and exclusions are recorded. Separate build and runtime evidence must identify this exact archive before a release claim can be considered. No canonical Release, deployment approval or completed SBOM is created by packaging.

The original committed-source web build compiled successfully but collection refused Next's outer-checkout nested server layout. The tracing correction was committed in `bec2202`; its fresh dependency installation reused all 368 packages offline, followed by successful contracts, Next/TypeScript and static-generation builds. This correction changes build isolation, not the business shell. NIN-25 remains open.

Actual collection from `bec2202` produced ZIP SHA-256 `ade943af07956152608738fafeea94241913f2e82011aacf3209c8e4131de77a`: 19,687 files and 453,704,482 payload bytes. All 589 committed inputs remained unchanged. The full per-file receipt is retained beside `.finai/artifacts/web-builds/web-artifact-u_ta58wy/web.zip`; its hash and compact build evidence are committed in `evidence/nin31-web-artifact.json`. Four focused checks passed, covering positive collection, changed source, dependency escape and Windows extended-length paths. Earlier failed collection attempts retain no success receipt.

The subsequent isolated HTTP check refused runtime acceptance: API readiness succeeded, but Node could not resolve `@swc/helpers/_/_interop_require_default`. Dereferencing pnpm junctions relocated Next away from its dependency lookup context. Both temporary processes were stopped; the active application was unchanged. The collected ZIP remains evidence of this failed candidate. The corrective package must preserve internally relocatable dependency links and bind their topology into the archive identity before another runtime check.

The corrected `g8-web-artifact/2` stores real traced files and 26 remapped directory-link declarations in an embedded `G8-WEB-MANIFEST.json`. Its archive hash binds both bytes and lookup topology. It refuses targets outside the traced physical tree. The verifier checks the embedded manifest against the sidecar and reconstructs only validated internal directory junctions after extracting regular files. It rejects cycles, ancestor targets and file/link collisions. The corrected ZIP is `e7e59f0a0a41409eb893f30763b6c9846d51f11aa632c72d6b009668d4408662`, with 1,300 files and 25,117,802 payload bytes. All committed source inputs still match. Six collector tests passed.

`scripts/verify-built-runtime.py` verified the exact installed API wheel and corrected web ZIP, launched hidden owned processes on 8063/3063, and checked API readiness (200), web root (200), anonymous web readiness (401), authenticated readiness (200) and authenticated session (200 with exact actor/scope equality). It uses fresh Python bytecode paths, keeps credentials out of command arguments, and stops only its owned processes in cleanup. Ten focused verifier checks passed. The actual GET-only receipt is `evidence/nin31-built-runtime-http.json`. No worker execution, full business browser journey, release promotion or completed SBOM is established. Active 8062/3062 services were preserved.
