# CANDIDATE constructions — not the ontology catalog

These files are instance proposals. They must not be merged into `platform_definitions()` and must not be installed as `APPROVED` / `PLATFORM_DEFINITION`.

| File | Contents |
|---|---|
| `coa-406.candidate.json` | 406 LocalAccount rows from ანგარიშები.xlsx, hash `607e37da…` matched |
| `seg-entities.candidate.json` | Public LegalEntity graph (SEG, SGP, SGG, SGGD, SOG, Telavgazi, Security, BST) |

Promote only through the existing reviewed-object workspace (submitter ≠ reviewer, receipt).
